<view class="container">
  <scroll-view class="store-scroll-view" scroll-y="true">
    <view class="store-list" style="position: relative; left: 20rpx;">
      <block wx:for="{{stores}}" wx:key="id">
        <view class="store-item" bindtap="onStoreSelect" data-id="{{item.id}}" data-name="{{item.name}}" data-selected="自提">
          <view class="store-info">
            <text class="store-name">{{item.name}}</text>
            <text class="store-address">{{item.address}}</text>
          </view>
        </view>
      </block>
    </view>
  </scroll-view>
</view>